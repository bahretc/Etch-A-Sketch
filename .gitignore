# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/
.venv/
venv/

# Tool output
output/

# written by run-app-windows.bat / run-app-mac.command when the repo root has no
# streamlit_app.py (a wheel-only install folder)
_launch_app.py

# per-study workspaces (real crash data; never committed)
studies/
